name: test-tag

on:
  pull_request:
  push:
    tags:
      - v*

jobs:
  test-tag:
    runs-on: ubuntu-latest
    
    steps:
      - name: Parse major version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          MM_TAG=$( echo "${{ github.ref_name}}" | grep -o "[0-9].[0-9]")
          echo "This is the parsed tag $MM_TAG"
